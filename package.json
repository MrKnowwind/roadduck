{
  "name": "phaser4-crossy-road",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "test": "node --test",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "phaser": "4.0.0"
  },
  "devDependencies": {
    "vite": "latest"
  }
}
